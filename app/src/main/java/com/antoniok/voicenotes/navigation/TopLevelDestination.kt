package com.antoniok.voicenotes.navigation

import com.antoniok.feature.notes.R as notesR

enum class TopLevelDestination(
    val titleTextId: Int
) {

    NOTES(
        titleTextId = notesR.string.notes
    )

}
